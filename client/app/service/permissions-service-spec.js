describe('permissionsService', function() {

  beforeEach(module('dgs'));

  it('should ...', inject(function(permissionsService) {

	//expect(permissionsService.doSomething()).toEqual('something');

  }));

});